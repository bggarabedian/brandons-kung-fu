# Local / secret files
*.local.*
denylist.local.txt
skills.sources.local.json
# but DO track the committed example template:
!skills.sources.local.example.json
# Cockpit bridge: the local config holds a private vault path; the example is tracked:
cockpit.local.json
!cockpit.local.example.json
.env

# Private project-source draft — keep out of this public repo (never track):
chatgpt-project-files/13-cinematic-web-build-pipeline.md

# Python
.venv/
venv/
__pycache__/
*.pyc

# Node
node_modules/

# Build output
dist/
build/

# Local install/test sandbox (install-claude --target .tmp/...)
.tmp/

# Runtime
ledger/

# Obsidian cockpit layer (private continuity; never tracked in this public repo)
.obsidian/
cockpit-vault/

# OS
.DS_Store
